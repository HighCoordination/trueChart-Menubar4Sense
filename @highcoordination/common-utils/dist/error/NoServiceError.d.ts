import { AUiError } from './AUiError';
/**
 * @class NoServiceError
 * throw this error when executing an action where service needed but there is no service
 */
export declare class NoServiceError extends AUiError {
}
